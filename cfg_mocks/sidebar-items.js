window.SIDEBAR_ITEMS = {"enum":[["MessageMock",""]],"macro":[["make_runtime_for_mock","Creates a runtime with a pallet mock to make isolated tests See tests below of this same file"]],"mod":[["pallet_mock_change_guard","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["pallet_mock_data","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["pallet_mock_fees","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["pallet_mock_liquidity_pools","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["pallet_mock_permissions","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["pallet_mock_pools","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["pallet_mock_rewards","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["pallet_mock_routers","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["pallet_mock_time","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["pallet_mock_try_convert","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["pallet_mock_write_off_policy","The module that hosts all the FRAME types needed to add this pallet to a runtime."],["reexport",""]],"struct":[["RouterMock","This wraps the mocking functionality of the pallet that we build here and is necessary since this will kept in storage, therefore it has to implement the below traits that make that possible."]]};